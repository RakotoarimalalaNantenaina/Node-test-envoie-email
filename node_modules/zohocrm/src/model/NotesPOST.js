(function(root, factory) {
  if (typeof define === 'function' && define.amd) {
    // AMD. Register as an anonymous module.
    define(['../ApiClient'], factory);
  } else if (typeof module === 'object' && module.exports) {
    // CommonJS-like environments that support module.exports, like Node.
    module.exports = factory(require('../ApiClient'));
  } else {
    // Browser globals (root is window)
    if (!root.Zohocrm) {
      root.Zohocrm = {};
    }
    root.Zohocrm.NotesPOST = factory(root.Zohocrm.ApiClient);
  }
}(this, function(ApiClient) {
  'use strict';

  /**
   * The NotesPOST model module.
   * @module model/NotesPOST
   * @version api-v2
   */

  /**
   * Constructs a new <code>NotesPOST</code>.
   * @alias module:model/NotesPOST
   * @class
   */
  var exports = function() {



  };

  /**
   * Constructs a <code>NotesPOST</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/NotesPOST} obj Optional instance to populate.
   * @return {module:model/NotesPOST} The populated <code>NotesPOST</code> instance.
   */
  exports.constructFromObject = function(data, obj) {
    if (data) { 
      obj = obj || new exports();

      if (data.hasOwnProperty('Note Content')) {
        obj['Note Content'] = ApiClient.convertToType(data['Note Content'], 'String');
      }
      if (data.hasOwnProperty('Title')) {
        obj['Title'] = ApiClient.convertToType(data['Title'], 'String');
      }
    }
    return obj;
  }


  /**
   * @member {String} Note Content
   */
  exports.prototype['Note Content'] = undefined;

  /**
   * @member {String} Title
   */
  exports.prototype['Title'] = undefined;




  return exports;
}));
